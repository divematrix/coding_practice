# 연습문제 27. 입력 값 검증

first_name = input("Enter the first name : ")

last_name = input("Enter the last name : ")

zip_code = input("Enter the ZIP code : ")

employee_id = input("Enter an employee ID : ")

def validateInput (str, fm):
	if fm == 'name':
		pass
	if fm == 'aa-1234':
		pass
	if fm == 'zip':
		pass